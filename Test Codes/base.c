#include<stdio.h>

int main()
{

        // int n, b, i = 0, count = 0;
        // printf("Input a number: ");
        // scanf("%d", &n);
        // printf("Base conversion: ");
        // scanf("%d", &b);

        // double x = n;
        // char s[32];

        // while(x>=1)
        // {
        //     x/=b;
        //     count++;
        // }
        // while(count)
        // {
        //     x*=b;
        //     if((int)x>=10) s[i] = 'A' + (int)x%10;
        //     else s[i] = '0' + (int)x;
        //     x-=(int)x;
        //     i++;
        //     count--;
        // }
        // s[i] = '\0';
        // printf("%s", s);

        // return 0;

}